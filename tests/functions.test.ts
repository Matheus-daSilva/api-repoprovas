import app from "../src/app.js"

describe("POST /authentication", () => {
    it("se o usuário não for cadastrado, retornar 201", async () => {
        const body = {
            email: 
        }
    })
})